# 当前目录开发规范

在继承上层 `AGENTS.md` 约束的基础上，当前目录额外执行以下开发规范。这些不是建议，是硬约束。


## 跨语言通用编码规范

以下规范仅适用于编程代码，不适用于文档、SQL 等非编程代码内容。

- 在满足需求的前提下，选择最简单的实现方案。
- 在不牺牲代码可读性的前提下，用最少的代码实现；禁止为了炫技、形式上的抽象或所谓“高级写法”引入不必要的复杂度。
- 实现代码时必须遵循高内聚、低耦合：将紧密相关的职责和数据收敛在同一模块内；模块之间仅通过最小、清晰且稳定的接口协作，禁止跨层访问内部实现、共享无明确所有权的可变状态或引入非必要依赖。
- 使用驼峰命名时，`ID` 一律写作 `Id`，例如使用 `UserId`、`userId`，禁止使用 `UserID`、`userID`。
- 编写代码注释时，禁止添加参考文档的引用和章节引用。

## 孤儿代码清理规范

- 修改代码时，如果发现 package、类型、函数、方法、字段、常量、配置或接口已经没有生产调用，或者只有测试在调用，必须同步删除。测试不是死代码的保留理由。
- 每次结束前检查本次新增内容是否拥有稳定生产入口。没有调用方、注册点或明确外部契约的代码必须立即清理，不得以“以后可能使用”为由保留。
- 删除孤儿代码时，同步删除只为它存在的测试、Mock、生成配置、测试数据和文档引用。未提交代码或明确不兼容旧行为的改动，不得新增墓碑测试或兼容壳。
- 删除前必须确认不存在接口实现、`init` 注册、反射、序列化字段、build tag、代码生成、外部 API、数据库映射、配置绑定、模板、脚本或 cgo 等隐式入口。无法确认时必须说明保留理由，不得猜测删除。

## MySQL 脚本规范

- MySQL 脚本不允许使用存储过程，禁止 `CREATE PROCEDURE`、`ALTER PROCEDURE`、`DROP PROCEDURE`、`CALL` 等过程相关语句。
- 数据迁移、数据修复和初始化脚本必须用普通 DDL/DML 表达。复杂逻辑应拆到应用代码或可审计的一次性脚本里，别把控制流藏进数据库。


## 执行原则

- **重要限制：**执行过程中涉及数据库、Kubernetes（K8s）及其他非代码资源时，除非用户明确要求对具体资源执行修改，否则只允许进行只读查询、检查和诊断；严禁擅自执行任何写入、创建、更新、删除、部署、迁移、扩缩容、重启或其他会改变资源状态的操作。
- 编写代码时，必须坚持极端实用主义：以解决当前明确问题为目标，优先选择最简单、直接、可验证且符合现有代码风格的方案；不为假设中的未来需求提前抽象，不引入没有实际收益的依赖、层级或复杂度；但不得以“实用”为由牺牲正确性、安全性和可维护性，或绕过本规范。
- 新代码必须遵守以上规则，旧代码在修改时同步向这些规则收敛。
- 代码评审时，以上条目按硬性规范处理，不当成“个人偏好”讨论。
- 如果确实有例外，必须有明确理由；没有理由的例外，本质上就是烂代码。



# 删除与清理规范
- 如果任务目标是删除或清理某项功能、代码、配置、接口或数据结构，完成标准是让其在代码库中如同从未出现过一样：彻底移除相关生产代码、兼容分支、适配层、废弃入口、配置、测试、Mock、测试数据、文档及引用。禁止为了保留旧行为、规避彻底删除或“防止以后加回来”而新增兼容代码、兜底逻辑、废弃标记或墓碑测试。
